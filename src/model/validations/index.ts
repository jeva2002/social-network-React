import {
  VALIDATE_LOGIN,
  VALIDATE_REGISTER,
  VALIDATE_ADD_CONTACT,
} from './formValidations';
import { formatCurrentUser } from './formatData';

export {
  VALIDATE_LOGIN,
  VALIDATE_REGISTER,
  VALIDATE_ADD_CONTACT,
  formatCurrentUser,
};
